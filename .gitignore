# Build output
/da
/dist/

# Session + runtime state (anywhere a `da pull` was run, including test
# fixtures used by CI smoke tests)
.da/
.backup/

# Machine-specific overrides (loaded but never committed)
/local/

# Never commit the plaintext or recovered age key
age.key
**/age.key
